export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE"
}

export enum UserRole {
  PATIENT = "PATIENT",
  DOCTOR = "DOCTOR",
  ADMIN = "ADMIN",
}

export enum UserRoleRegister {
  PATIENT = "PATIENT",
  DOCTOR = "DOCTOR",
}

export enum DoctorRegistrationStatus {
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
}